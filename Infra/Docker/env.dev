# .env.dev 파일
# Staging environment (Default)
ENV_VENDOR_FILE=env.dev

# 어플리케이션 이름
APP_NAME=tsp_protocol_gateway

JAVA_OPTS="-Dfile.encoding=UTF-8 -Dspring.profiles.active=dev -Dlogging.level.org.springframework=DEBUG"

# Spring 프로파일 (ex: dev, prod, local 등)
SPRING_PROFILE=dev

# 어플리케이션 포트
PORT_NUM=8200
APP_PORT=8200
#APP_PORT=8200

#
VIRTUAL_HOST=tsp.gw.engistech.com
VIRTUAL_PORT=${PORT_NUM}
LETSENCRYPT_HOST=tsp.gw.engistech.com
LETSENCRYPT_EMAIL=pyungsun.park@engistech.com